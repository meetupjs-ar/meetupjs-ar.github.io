{
  "name": "meetupjs-ar",
  "version": "1.0.0",
  "scripts": {
    "deploy": "gh-pages -b master -d ./public",
    "start-dev": "browser-sync start --port 6590 --server './public' --files './public' --no-open --no-ui"
  },
  "author": "durancristhian@gmail.com",
  "license": "MIT",
  "devDependencies": {
    "browser-sync": "^2.18.6",
    "gh-pages": "^0.12.0"
  }
}
